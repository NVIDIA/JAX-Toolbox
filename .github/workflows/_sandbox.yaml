name: "~Sandbox"

on:
  push:
  workflow_dispatch:

jobs:

  build-jax:
    uses: ./.github/workflows/_build.yaml
    with:
      ARCHITECTURE: amd64
      ARTIFACT_NAME: artifact-jax-build
      BADGE_FILENAME: badge-jax-build
      BUILD_DATE: 2024-04-18
      BASE_IMAGE: ghcr.io/nvidia/jax:base
      CONTAINER_NAME: jax
      DOCKERFILE: .github/container/Dockerfile.jax
      RUNNER_SIZE: large
    secrets: inherit