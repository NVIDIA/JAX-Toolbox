name: "~Sandbox"

on:
  workflow_dispatch:

jobs:
  build-base:
    uses: ./.github/workflows/_build_base.yaml
    with:
      ARCHITECTURE: amd64
      JAX_NCCL_VERSION: "2.20.3-1"
    secrets: inherit

  build-base:
    uses: ./.github/workflows/_build_base.yaml
    with:
      ARCHITECTURE: amd64
      NCCL_VERSION: "2.20.5-1"
    secrets: inherit


  build-base-default:
    uses: ./.github/workflows/_build_base.yaml
    with:
      ARCHITECTURE: amd64
    secrets: inherit
