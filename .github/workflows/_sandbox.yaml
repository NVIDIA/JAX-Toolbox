name: "~Sandbox"

on:
  workflow_dispatch:

jobs:
  build-base:
    uses: ./.github/workflows/_build_base.yaml
    with:
      ARCHITECTURE: amd64
      BUILD_DATE: "2024-14-25"
      MANIFEST_ARTIFACT_NAME: base
    secrets: inherit

