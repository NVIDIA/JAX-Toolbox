name: "~Sandbox"

on:
  workflow_dispatch:

jobs:
  test-levanter:
    needs: build-levanter
    uses: ./.github/workflows/_test_unit.yaml
    with:
      IMAGE: ghcr.io/nvidia/jax:levanter #${{ needs.build-levanter.outputs.DOCKER_TAG_FINAL }}
      TEST_NAME: levanter
      EXECUTE: |
        docker run \
        --gpus all \
        --shm-size=1g ${{ needs.build-levanter.outputs.DOCKER_TAG_FINAL }} \
        bash -ec \ 
        "pip install pytest test-utils && PYTHONPATH=/opt/levanter/tests:$PYTHONPATH pytest /opt/levanter/tests" \
        | tee test-levanter.log
      STATISTICS_SCRIPT: |
        summary_line=$(tail -n1 test-levanter.log)
        errors=$(echo $summary_line | grep -oE '[0-9]+ error' | awk '{print $1} END { if (!NR) print 0}')
        failed_tests=$(echo $summary_line | grep -oE '[0-9]+ failed' | awk '{print $1} END { if (!NR) print 0}')
        passed_tests=$(echo $summary_line | grep -oE '[0-9]+ passed' | awk '{print $1} END { if (!NR) print 0}')
        total_tests=$((failed_tests + passed_tests))
        echo "TOTAL_TESTS=${total_tests}" >> $GITHUB_OUTPUT
        echo "ERRORS=${errors}" >> $GITHUB_OUTPUT
        echo "PASSED_TESTS=${passed_tests}" >> $GITHUB_OUTPUT
        echo "FAILED_TESTS=${failed_tests}" >> $GITHUB_OUTPUT
      ARTIFACTS: |
        test-levanter.log
      ARTIFACT_NAME: artifact-levanter-unit-test
      BADGE_FILENAME: badge-levanter-unit-test
    secrets: inherit    
