name: "~Sandbox"

on:
  push:
  workflow_dispatch:

jobs:
  build-base:	
    uses: ./.github/workflows/_build_base.yaml
    with:
      ARCHITECTURE: amd64
      JAX_LIBNCCL_PACKAGE: 2.20.5-1+cuda12.4
    secrets: inherit	        

  # build-base-default-arm64:	
  #   uses: ./.github/workflows/_build_base.yaml
  #   with:
  #     ARCHITECTURE: arm64	
  #   secrets: inherit

  # build-base-default-amd64:	
  #   uses: ./.github/workflows/_build_base.yaml
  #   with:
  #     ARCHITECTURE: amd64	
  #   secrets: inherit
