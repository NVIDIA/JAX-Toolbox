name: "~Sandbox"

on:
  workflow_dispatch:

jobs:
  sandbox:
    runs-on: ubuntu-22.04
    steps:
      - name: Get data from runner
        shell: bash -x -e {0}
        run: |
          echo GITHUB_WORKSPACE=$GITHUB_WORKSPACE
          df -h
          ls -lah $GITHUB_WORKSPACE
