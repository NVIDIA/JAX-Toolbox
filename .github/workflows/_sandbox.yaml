name: "~Sandbox"

on:
  workflow_dispatch:

jobs:
  runner:
    uses: ./.github/workflows/_runner_ondemand_slurm.yaml
    with:
      NAME: "TEST-${{ github.run_id }}"
      LABELS: "TEST:${{ github.run_id }}"
      TIME: "01:00:00"
    secrets: inherit
