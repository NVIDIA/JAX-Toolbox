ARG BASE_IMAGE=ghcr.io/nvidia/jax-toolbox:base
FROM ${BASE_IMAGE}

RUN git clone https://github.com/google/jax.git /opt/jax && \
    cd /opt/jax && \
    pip install --upgrade -e ".[cuda]" -f https://storage.googleapis.com/jax-releases/jax_cuda_releases.html

ADD test-jax.sh /usr/local/bin