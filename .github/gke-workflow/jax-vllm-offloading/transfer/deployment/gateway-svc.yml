apiVersion: v1
kind: Service
metadata:
  labels:
    app: jax-vllm-gateway
  name: jax-vllm-gateway
spec:
  ports:
  - port: 80
    protocol: TCP
    targetPort: 50051
  selector:
    app: jax-vllm-gateway
  type: ClusterIP
